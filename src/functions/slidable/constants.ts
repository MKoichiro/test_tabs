import { SlidableParamsOptionalDefault } from './types';

export const defaultValForOptionalParams: SlidableParamsOptionalDefault = {
    GRADIENT_THRESHOLD: 0.3,
    COMPLEMENT_ANIME_DURATION: 200,
    SLIDABLE_PLAY: 20,
};
